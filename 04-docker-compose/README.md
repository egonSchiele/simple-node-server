Get it running:

With docker desktop:
```bash
docker compose up
```

Without:

```bash
brew install docker-compose
docker-compose up
```

Then open [http://localhost:80](http://localhost:80) to see your app.